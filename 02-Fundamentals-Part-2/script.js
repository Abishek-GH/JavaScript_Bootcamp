'use strict';

function functionName(paramter) {
    console.timeLog("Hey");
}

functionName(paramter);

const thisFunction = function (Paramter) {
    return "Hey";
}

thisFunction(Paramter);

const hello = paramter => "Result";
console.log(hello(Paramter));

const hi = (paramter1, paramter2) => {
    return 'Hii';
}