/*let res = 50;
// Used to get the type of the value
console.log(typeof res);

// Undefined means the value is not assigne din the variable or the variable doesnt hold any value;
// Typeof undefined is undefined only
let temp;
console.log(typeof temp);

// Type of null should be null, but we get it as an object (It is a bug)
// But Javscript didn't fixed this bug for legacy reasons
// Javascript says that type of null is an object
console.log(typeof null);


let age = 30;
age = 31; // Mutable

const birthYear = 1991;
//birthYear = 1990; // Inmutable
//const job; // Can't intialise a variable with const


let x, y;
x = y = 25 - 10 - 5;
console.log(x, y);


const year = '1991';
const year2 = Number(year);
console.log(typeof year);
console.log(typeof year2);


const myName = "abishek";
const myName2 = Number(myName);
console.log(typeof myName);
console.log(typeof myName2);

const day = "wednesday"
switch (day) {
    case 'monday':
        console.log("Monday");
        break;
    case 'tuesday':
        console.log("Tuesday");
        break;
    case 'wednesday':
    default:
        console.log('NA');
}

const age = 29;
age >= 30 ? console.log('Hello') : console.log("Hey");

*/